// Centralized model config. Groq rotates/deprecates models periodically —
// if this one ever gets deprecated, update it here once instead of hunting
// through every agent file.
export const GROQ_MODEL = 'openai/gpt-oss-120b';